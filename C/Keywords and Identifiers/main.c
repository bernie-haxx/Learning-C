#include <stdio.h>

int main(int argc, char const *argv[])
{
	/*
	Character set

	This is a set of alphabets,letters and some special characters that are valid in C language
	Alphabets
	Uppercases and lower cases
	C accepts both of them

	And also Digits

	And also special Characters like alphanumerics

	Keywords

	These are predefined words used in programmming that have special meanings to the compiler.
	for example int money;
	
	Examples of the keywords:
	auto
	double
	int 
	struct
	break
	else 
	long
	switch
	case
	enum
	register
	typedef
	char
	extern
	return
	union
	continue
	for 
	signed
	void
	do
	if
	static
	while
	default 
	go
	sizeof
	volatile
	const
	float
	short
	unsigned

	Identifiers
	These are the namings based on variable representation
	Rules::
	1. A valid identifier can have letters(bith uppercase and lowercase letters, digits and underscores
	2. The first letter of an identifier should be either a letter or an underscore.
	3. There is no rule on how long an identifier can be. However, you may run into problems in some compilers if identifier is longer than 31 characters.)
	 
	*/
	printf("%s\n", "red" );
	return 0;
}